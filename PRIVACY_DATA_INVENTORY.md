# CornellPulse data inventory

This inventory describes behavior visible in the repository as of 2026-08-02. Deployment infrastructure, backups, provider settings, and operator practices must be verified separately. It is not a claim of anonymity or legal compliance.

| Location / processor | Data | Purpose | Trigger and consent | Retention visible in code | Deletion / withdrawal |
| --- | --- | --- | --- | --- | --- |
| Browser `localStorage` | `cornellpulse_onboarded` | Remember onboarding completion | Necessary preference set when onboarding finishes | Until cleared, browser/app data is removed, or the app is uninstalled | Reset onboarding or use Privacy & Data device clearing |
| Browser page memory | Current mood, sleep/workload categories, triggers, talk preference, optional free text, and college while a check-in is open | Generate a recommendation locally | Entered during a check-in; no server transmission is required | Until submission, deletion, navigation, or page close; free text is cleared immediately after recommendation generation and is not restored after reload | Visible **Delete this check-in** action, navigation, or page close |
| Browser `localStorage` | `cornellpulse_history`: up to 20 random local IDs, dates, mood scores, chosen resource IDs, and chosen resource names | Save a user-selected next-step plan and show local check-in history | Created only after the user chooses an option and selects **Save this plan on this device**; it never contains free text, triggers, talk preference, or feedback | Up to 20 entries, with no time expiry | Result-level **Delete this check-in**, Profile history clearing, or Privacy & Data device clearing |
| Browser page memory | Optional results feedback (`helpful` or `not helpful`) | Adjust the current result-page acknowledgement | User explicitly selects an answer; it is not transmitted or persisted | Until navigation, refresh, or page close | Navigate away, refresh, or close the page |
| Browser `localStorage` | `cornellpulse_privacy_preferences`: aggregate and resource-analytics choices | Remember opt-in/withdrawal choices | Both choices default off; changed explicitly by the user | Until cleared | Turn either choice off for future activity, or clear device data |
| Browser `sessionStorage` | `cornellpulse_checkin_draft_v2`: current step, mood, sleep/workload categories, triggers, talk preference, and college. It does not contain optional free text | Preserve valid structured choices across Back and reload in the same tab | Created while a check-in is in progress; no server transmission is required | Until submission, visible deletion, tab/session close, or Privacy & Data device clearing | Visible **Delete this check-in**, finish the check-in, close the tab/session, or use Privacy & Data device clearing. Legacy draft/result keys are also cleared for compatibility |
| Browser cookie | Signed administrator token | Authenticate administrator API requests | Administrator login | 15 minutes by default; HttpOnly, SameSite=Strict, Secure in production | Logout or expiry |
| Check-in aggregate API process memory | Mood score, sleep category, workload category, and college category | Add an explicitly opted-in contribution to combined hourly totals | Called only after the local aggregate preference is enabled | Request-processing duration in application code; infrastructure may retain technical logs | Turning the preference off stops future calls; provider logs require operator/provider action |
| Redis | No active Redis integration remains in the current application; the prior check-in deduplication key and Redis configuration were removed | Not used | Not applicable | Not applicable | Not applicable |
| PostgreSQL `college_hour_aggregates` | Hourly college, count and sums for mood/sleep/workload. Legacy distress/resource columns may remain in the schema but the current aggregate endpoint does not populate them | Campus trend aggregation | Only when aggregate contribution is explicitly enabled | No automatic expiry in current code | Future contribution can be stopped; an earlier contribution cannot be isolated from a combined total |
| PostgreSQL `campus_hour_aggregates` | Hourly campus count and sums for mood/sleep/workload | Campus trend aggregation | Only when aggregate contribution is explicitly enabled | No automatic expiry in current code | Same limitation as college aggregates |
| PostgreSQL `resource_clicks` | Resource identifier, action (`call`/`website`), timestamp | Optional product analytics | Only when resource-click analytics is explicitly enabled | No automatic expiry in current code | Turning the choice off stops future events; existing rows require operator deletion and are not tied to an account |
| PostgreSQL `peer_signups` | Supporter name, email, phone, year, major, locations, availability, interests, about text; reference name, phone, email, relationship; approval status and time | Review and publish supporter profiles | Submitted only if supporter signup is enabled | No automatic expiry in current code | Authorized administrator deletion; submitter/reference must contact operator to locate and request access/correction/deletion |
| PostgreSQL `peer_connect_requests` | Supporter name; requester name, email, optional phone; location/time preferences; message; moderation status and time | Facilitate and moderate connection requests | Submitted only if Peer Connect is enabled | No automatic expiry in current code | Authorized administrator deletion; requester must contact operator |
| PostgreSQL `supporter_reports` | Supporter name, optional reporter email, report reason, resolved status and time | Safety reporting and moderation | Submitted only if Peer Connect is enabled | No automatic expiry in current code | Authorized administrator deletion; reporter or subject must contact operator |
| PostgreSQL `push_subscribers` model | Push subscription object, optional college, subscription and notification times | Intended push notifications | No active collection route found in this repository | No automatic expiry in model | Requires operator/database action if collection is later enabled |
| PostgreSQL `academic_calendar_events` model | Event type/name/dates/colleges/fetch time | Intended calendar context | Not user-supplied; no active collection route found | No automatic expiry in model | Operator/database action |
| Resend and administrator email mailbox | Supporter and reference details; approval notices; connection-request details; safety-report details | Notify operator about peer workflows | Occurs only in enabled peer workflows when email is configured | Not controlled by application code; governed by provider and mailbox settings | Database deletion does not delete email copies; operator/provider deletion is required |
| Application and hosting logs | At minimum method, path, status, timestamp, IP/network metadata, and user agent may be recorded by servers or proxies; application currently prints email delivery results/failures but does not intentionally print check-in bodies | Operations, security, debugging | Generated by infrastructure/operator configuration | Not defined in repository | Requires operator/provider log and backup procedures |
| External phone, SMS, and web services | Destination, device/network metadata, and any information the user gives the external service | Access crisis, Cornell, and community resources | User follows a phone/SMS/web link | Controlled by the external service | Request directly from that service under its practices |

## Current analytics boundary

CornellPulse has no third-party browser analytics SDK in this repository. Optional resource-click events are first-party database records. They exclude names, email addresses, check-in text, and a CornellPulse account identifier, but the project has not established that infrastructure metadata cannot be identifying.

## Unresolved governance requirements

- Set, document, and implement deletion schedules for identifiable peer records, resource-click events, aggregates, email, backups, and logs.
- Configure and monitor `VITE_PRIVACY_CONTACT_EMAIL` before enabling identifiable-data features.
- Establish identity-verification and authorization procedures for access, correction, withdrawal, and deletion requests.
- Execute processor/vendor review and data-processing terms for hosting, database, Resend, logging, monitoring, and backups.
- Decide whether aggregate dimensions and minimum-count publication thresholds are sufficient to manage re-identification risk.
- Review wellness/crisis text processing, supporter/reference consent, and record-retention duties with qualified counsel and Cornell stakeholders.
