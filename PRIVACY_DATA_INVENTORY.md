# CornellPulse data inventory

This inventory describes behavior visible in the repository as of 2026-08-02. Deployment infrastructure, backups, provider settings, and operator practices must be verified separately. It is not a claim of anonymity or legal compliance.

| Location / processor | Data | Purpose | Trigger and consent | Retention visible in code | Deletion / withdrawal |
| --- | --- | --- | --- | --- | --- |
| Browser `localStorage` | `cornellpulse_onboarded` | Remember onboarding completion | Necessary preference set when onboarding finishes | Until cleared, browser/app data is removed, or the app is uninstalled | Reset onboarding or use Privacy & Data device clearing |
| Browser page memory | Current mood, sleep/workload categories, triggers, talk preference, optional free text, and college while a check-in is open | Generate a recommendation locally | Entered during a check-in; no server transmission is required | Until submission, deletion, navigation, or page close; free text is cleared immediately after recommendation generation and is not restored after reload | Visible **Delete this check-in** action, navigation, or page close |
| Browser `localStorage` | `cornellpulse_history`: up to 20 random local IDs, dates, mood scores, chosen resource IDs/names, plan status, optional reminder time, and optional contacted/fit answers | Save and follow up on a user-selected next-step plan; render a local mood trend | Created only after the user chooses and saves a plan; follow-up fields require separate user actions; it never contains free text, triggers, talk preference, name, email, or phone | Default 90 days and at most 20 entries; user can choose 30 days, 1 year, or keep-until-deleted | Delete an entry, clear history, shorten retention, or clear all device data; JSON export is available locally |
| Browser `localStorage` | `cornellpulse_history_settings`: retention choice | Apply one consistent history-retention period | Changed explicitly in **History & Privacy**; defaults to 90 days | Until changed or device data is cleared | Change the retention choice or clear device data |
| Browser `localStorage` | `cornellpulse_local_measurement`: counters for check-in completion, resource-action types, successful contact, and repeat use | Privacy-minimized product measurement | Written only after the separate product-measurement choice is enabled; no event timestamps, resource IDs, raw answers, free text, names, emails, or phone numbers | Until device data is cleared; not uploaded by current code | Turn consent off to stop future counts; clear device data to delete counters |
| Browser page memory | Optional results feedback (`helpful` or `not helpful`) | Adjust the current result-page acknowledgement | User explicitly selects an answer; it is not transmitted or persisted | Until navigation, refresh, or page close | Navigate away, refresh, or close the page |
| Browser `localStorage` | `cornellpulse_privacy_preferences`: aggregate, resource-analytics, and local product-measurement choices | Remember opt-in/withdrawal choices | All choices default off; changed explicitly by the user | Until cleared | Turn a choice off for future activity, or clear device data |
| Browser `sessionStorage` | `cornellpulse_checkin_draft_v2`: current step, mood, sleep/workload categories, triggers, talk preference, and college. It does not contain optional free text | Preserve valid structured choices across Back and reload in the same tab | Created while a check-in is in progress; no server transmission is required | Until submission, visible deletion, tab/session close, or Privacy & Data device clearing | Visible **Delete this check-in**, finish the check-in, close the tab/session, or use Privacy & Data device clearing. Legacy draft/result keys are also cleared for compatibility |
| Browser cookie | Signed administrator token | Authenticate administrator API requests | Administrator login | 15 minutes by default; HttpOnly, SameSite=Strict, Secure in production | Logout or expiry |
| Check-in aggregate API process memory | Mood score, sleep category, workload category, and college category | Add an explicitly opted-in contribution to combined hourly totals | Called only after the local aggregate preference is enabled | Request-processing duration in application code; infrastructure may retain technical logs | Turning the preference off stops future calls; provider logs require operator/provider action |
| Redis | No active Redis integration remains in the current application; the prior check-in deduplication key and Redis configuration were removed | Not used | Not applicable | Not applicable | Not applicable |
| PostgreSQL `college_hour_aggregates` | Hourly college, count and sums for mood/sleep/workload. Legacy distress/resource columns may remain in the schema but the current aggregate endpoint does not populate them | Campus trend aggregation | Only when aggregate contribution is explicitly enabled | No automatic expiry in current code | Future contribution can be stopped; an earlier contribution cannot be isolated from a combined total |
| PostgreSQL `campus_hour_aggregates` | Hourly campus count and sums for mood/sleep/workload | Campus trend aggregation | Only when aggregate contribution is explicitly enabled | No automatic expiry in current code | Same limitation as college aggregates |
| PostgreSQL `resource_clicks` | Resource identifier, action (`call`/`website`), timestamp | Optional product analytics | Only when resource-click analytics is explicitly enabled | No automatic expiry in current code | Turning the choice off stops future events; existing rows require operator deletion and are not tied to an account |
| PostgreSQL `peer_signups` | Immutable supporter UUID; public display name, year, major, locations, availability, interests, and profile text; encrypted applicant email/phone; credential hash; application state; policy version/time; hashed identity and training evidence references; completion and reviewer timestamps | Operate gated supporter onboarding without exposing private contact, identity, reference, training, or review information publicly | Draft creation and explicit policy acceptance only if supporter signup is enabled; feature remains disabled by default | 365 days by default; expired-record purge must be scheduled | Authenticated withdrawal erases contact, credential, and identity-subject data and removes the profile publicly; administrator deletion pseudonymizes the public profile while retaining time-limited non-PII audit/status evidence |
| PostgreSQL `supporter_reference_invitations` | Immutable invitation/supporter UUIDs, keyed token hash, encrypted invitee email, encrypted relationship/statement after explicit consent, invitation status and timestamps; no phone number | Deliver a single-use consent request and allow the reference to accept or decline before any substantive response is collected | Applicant or administrator supplies only the delivery email after identity review; the reference must explicitly consent before relationship/statement content is accepted | 14-day invitation expiry by default; expired-record purge must be scheduled | Decline erases the delivery email; supporter withdrawal, administrator deletion, or retention purge revokes the invitation and erases email/response payloads |
| PostgreSQL `peer_requesters` | Immutable requester UUID, bcrypt credential hash, encrypted name/email/optional phone, status, hashed identity-evidence reference and verification timestamps | Authenticate and identity-gate requesters without placing private contact details in public profiles, bearer tokens, or connection records | Registration only if Peer Connect is enabled; connection actions additionally require an identity-verification record | 90 days by default | Authenticated withdrawal replaces encrypted PII with a non-identifying tombstone, invalidates the credential, clears identity evidence, and cancels active connections |
| PostgreSQL `peer_connect_requests` | Immutable request, supporter, and requester UUIDs; encrypted approved public-location ID, safe meeting-window ID, and optional contact-free note; requester/supporter consent, expiry, status, and lifecycle timestamps | Operate the double-opt-in connection lifecycle without copying either participant's contact information | Verified authenticated requester plus explicit request consent; relay requires separate verified supporter acceptance | 90 days by default | Requester may cancel; supporter may decline, expire, or block; staff may mark unavailable; administrator deletion or retention purge erases the encrypted payload and preserves only time-limited non-PII audit/status evidence |
| PostgreSQL `peer_relay_messages` | Connection UUID, sender role/UUID, encrypted contact-free message body, timestamps | Provide an in-app relay only after both participants opt in | Verified authenticated participant in an accepted connection; server rejects recognizable email, phone, URL, and social-handle content | 90 days by default | Administrator connection deletion or retention purge tombstones the encrypted body; provider backups may follow a separate schedule |
| PostgreSQL `peer_blocks` | Supporter/requester UUID pair, creator role/UUID, active state, timestamps | Prevent new connection requests after a supporter blocks a requester | Authenticated addressed supporter action | 365 days by default | Retention purge deactivates and tombstones the block; operator review is required before offering unblock controls |
| PostgreSQL `peer_connection_reports` | Connection UUID, reporter and target role/UUIDs, encrypted reason, moderation status/timestamps | Let either participant report a safety concern without emailing report content | Authenticated connection participant action | 365 days by default | Resolution preserves the encrypted report until deletion/retention; administrator connection deletion or retention purge tombstones the reason |
| PostgreSQL `supporter_reports` | Immutable report, supporter, and reporter UUIDs; encrypted report reason; moderation status | Safety reporting and moderation | Authenticated requester action only when Peer Connect is enabled | 365 days by default | Administrator deletion or retention purge erases encrypted content; status/audit evidence is retained only for its configured period |
| PostgreSQL `peer_audit_logs` and `peer_status_history` | Actor role and immutable ID, action/status code, target type and immutable ID, non-PII metadata, timestamps | Security review, accountability, and status provenance | Written by authenticated peer workflow actions | 365 days by default; purge endpoint removes expired records | Administrator-triggered retention purge; application routes do not expose mutation of individual audit events |
| PostgreSQL `rate_limit_buckets` | Scope, keyed hash of IP or actor identifier, fixed-window count and expiry | Shared abuse controls across API processes | Login, registration, connection, and report attempts | Window expiry is configured; administrator retention purge removes expired rows | Database cleanup after expiry |
| PostgreSQL `push_subscribers` model | Push subscription object, optional college, subscription and notification times | Intended push notifications | No active collection route found in this repository | No automatic expiry in model | Requires operator/database action if collection is later enabled |
| PostgreSQL `academic_calendar_events` model | Event type/name/dates/colleges/fetch time | Intended calendar context | Not user-supplied; no active collection route found | No automatic expiry in model | Operator/database action |
| Resend and administrator/reference email mailbox | Recipient address and either a fixed immutable workflow reference or single-use reference consent link; no application statement, report reason, requester contact, or reference response | Notify an operator/supporter or deliver a reference consent invitation | Occurs only in enabled peer workflows when email is configured; opening a reference invitation does not constitute consent | Not controlled by application code; governed by provider and mailbox settings | Database deletion does not delete provider/mailbox copies; operator/provider deletion is required |
| Application and hosting logs | At minimum method, path, status, timestamp, IP/network metadata, and user agent may be recorded by servers or proxies; peer email failures intentionally omit recipient, payload, and user content | Operations, security, debugging | Generated by infrastructure/operator configuration | Not defined in repository | Requires operator/provider log and backup procedures |
| External phone, SMS, and web services | Destination, device/network metadata, and any information the user gives the external service | Access crisis, Cornell, and community resources | User follows a phone/SMS/web link | Controlled by the external service | Request directly from that service under its practices |

## Current analytics boundary

CornellPulse has no third-party browser analytics SDK in this repository. Optional resource-click events are first-party database records. They exclude names, email addresses, check-in text, and a CornellPulse account identifier, but the project has not established that infrastructure metadata cannot be identifying.

The separate product-measurement plan currently stays on-device. With explicit consent it counts check-in completions, resource-action types, successful-contact answers, and repeat saves. It does not record event timestamps, resource identifiers, raw answers, free text, names, emails, or phone numbers. Any future upload of these counters requires a new documented data-flow review; the current consent copy does not authorize transmission.

## Unresolved governance requirements

- Deploy and schedule the implemented peer retention purge; separately set deletion schedules for resource-click events, aggregates, email, backups, and infrastructure logs.
- Configure and monitor `VITE_PRIVACY_CONTACT_EMAIL` before enabling identifiable-data features.
- Complete the authorized Cornell OIDC/SAML integration and establish identity-verification and authorization procedures for access, correction, withdrawal, and deletion requests. Manual non-production identity evidence is not launch-ready verification.
- Execute processor/vendor review and data-processing terms for hosting, database, Resend, logging, monitoring, and backups.
- Decide whether aggregate dimensions and minimum-count publication thresholds are sufficient to manage re-identification risk.
- Review wellness/crisis text processing, supporter/reference consent, and record-retention duties with qualified counsel and Cornell stakeholders.
